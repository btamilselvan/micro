server:
 port: 8080
zuul:
  ignoredServices: '*'
  routes:
    address:
      path: /address/**
      url: http://address-service.local:8080/
      serviceId: address-service
    person:
      path: /person/**
      serviceId: person-service
